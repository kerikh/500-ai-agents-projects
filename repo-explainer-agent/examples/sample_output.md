# Junior Onboarding Guide: `sample_app`

_Generated by Repo Explainer Agent (offline heuristic) on 2026-08-29 11:26 UTC_

## What is this project?

This repository (`sample_app`) looks like a Python codebase. Detected languages: Python. Frameworks/libraries: FastAPI, pytest.

README gist: A tiny FastAPI-style learning app used as a **fixture** for the Repo Explainer Agent.

**sample_app** is primarily a Python project. It leans on FastAPI, pytest. As a newcomer, think of it as folders like src, tests, with execution often starting from `src/app.py`. Read the README first, then open one entry point and follow the imports.

## Welcome note

Welcome! You do not need to understand every file in `sample_app` on day one. Start with the README, install dependencies, then open one entry-point file and trace what it imports. Use tests as living documentation. Ask questions about anything that feels magical — those are usually the best learning moments.

## Tech stack at a glance

- **Primary language:** Python
- **Languages:** Python
- **Frameworks / libraries:** FastAPI, pytest
- **Package managers:** pip/uv/poetry (Python)
- **Tooling:** none detected
- **Has tests:** yes

## Folder map (start here)

```text
sample_app/
├── src/
│   ├── __init__.py
│   ├── app.py
│   └── notes.py
├── tests/
│   └── test_notes.py
├── README.md
└── requirements.txt
```

## What each top-level piece is for

- `README.md` (file): Start here — project purpose and setup.
- `requirements.txt` (file): Python dependencies to install.
- `src` (dir): Application source code — usually the heart of the project.
- `tests` (dir): Automated tests — great place to learn expected behavior.

## Mental model

**sample_app** is primarily a Python project. It leans on FastAPI, pytest. As a newcomer, think of it as folders like src, tests, with execution often starting from `src/app.py`. Read the README first, then open one entry point and follow the imports.

## How a run typically flows

1. Developer or user starts from README / docs.
2. Dependencies are installed via the package manager signals found in the repo.
3. Runtime begins at `src/app.py` (or the documented start command).
4. Tests describe expected behavior — run them early to learn by example.
5. Follow imports/calls from the entry point into modules (src/app/lib/api).

## Suggested learning path

### Step 1: Read the README

- **Why:** It explains the project's purpose and how to run it.
- **Do:** Open `README.md` and skim headings: Sample Notes API, Quick start, Tests, What it does

### Step 2: Install dependencies

- **Why:** You need a working environment before reading runtime code.
- **Do:** README mentions `pip install` — start there for Python setup.; Install Python deps with: `pip install -r requirements.txt`

### Step 3: Find the entry point

- **Why:** Entry points show how the program boots.
- **Do:** Inspect: `src/app.py`

### Step 4: Walk one request/path through the code

- **Why:** Following a single flow beats reading folders randomly.
- **Do:** Use the mental model and data-flow bullets in this report as a map.

### Step 5: Run the tests

- **Why:** Tests show expected behavior with concrete examples.
- **Do:** Run the project's test command (pytest, npm test, go test, etc.).


## Quick-start hints from the repo

- README mentions `pip install` — start there for Python setup.
- Install Python deps with: `pip install -r requirements.txt`

## Likely entry points

- `src/app.py`

## Sample file sketches

- src/app.py → imports/uses: from __future__ import annotations; from .notes import NotesStore · defines: def main() -> None:
- Empty or unreadable file.
- src/notes.py → imports/uses: from __future__ import annotations · defines: class NotesStore:
- requirements.txt → preview: fastapi==0.115.0 | pytest==8.3.3
- tests/test_notes.py → imports/uses: from src.notes import NotesStore · defines: def test_add_and_list_notes():

## Glossary

- **Repository (repo):** The project folder tracked by Git — all the code and docs live here.
- **Entry point:** The file or command where the program starts running.
- **Dependency:** An external library your project needs (listed in requirements.txt, package.json, etc.).
- **Module / package:** A folder or file that groups related code so other files can import it.
- **FastAPI:** A library/framework detected in this repo's manifests — look it up in the official docs while reading related code.
- **pytest:** A library/framework detected in this repo's manifests — look it up in the official docs while reading related code.
- **src/app.py:** Likely boot/start file for this project — open it early.

## Common junior pitfalls

- Trying to read every file on day one — prefer one vertical slice instead.
- Skipping the README and guessing install commands.
- Editing code before you can run the existing demo/tests.
- Installing packages globally instead of using a virtual environment (venv/uv).

## First-week plan

- Day 1: Clone, install, run the documented demo or tests.
- Day 2: Read README + top-level folders; sketch your own box-and-arrow diagram.
- Day 3: Trace one entry point (src/app.py) through 2–3 imports.
- Day 4: Pair on a tiny bugfix or docs typo to practice the change workflow.
- Day 5: Explain the repo aloud (rubber-duck) using this onboarding guide; note remaining questions.

---

Tip: keep this guide next to the repo and update it as you learn. Teaching someone else is the fastest way to lock the map into memory.
